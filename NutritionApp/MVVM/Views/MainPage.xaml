<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="NutritionApp.MainPage"
             xmlns:viewmodel="clr-namespace:NutritionApp.MVVM.Viewmodels"       
             x:DataType="viewmodel:MainViewModel"
             xmlns:model="clr-namespace:NutritionApp.MVVM.Models"
             xmlns:toolkit="http://schemas.microsoft.com/dotnet/2022/maui/toolkit"
             BackgroundColor="{DynamicResource White}">

    <ScrollView>
        <VerticalStackLayout
            Spacing="10"
            Padding="20,0">

            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"></ColumnDefinition>
                    <ColumnDefinition Width="*"></ColumnDefinition>
                    <ColumnDefinition Width="*"></ColumnDefinition>
                </Grid.ColumnDefinitions>
                <Button Grid.Column="0" Text=">"></Button>
                <Label FontAttributes="Bold" HorizontalOptions="Center" VerticalOptions="Center" Grid.Column="1" Text="2023-08-29"></Label>
                <Button Grid.Column="2" Text=">"></Button>
            </Grid>

            <VerticalStackLayout>
                <SearchBar x:Name="searchBar"
                       SearchCommand="{Binding PerformSearchCommand}"
                       SearchCommandParameter="{Binding Text, Source={x:Reference searchBar}}"
                       TextColor="Black"
                       BackgroundColor="LightGrey"
                       Placeholder="Search foods..."/>

                <ListView x:Name="searchResults" 
                      ItemsSource="{Binding SearchResults}"
                      SelectionMode="Single"
                      ItemSelected="SearchResultSelected">
                    <ListView.ItemTemplate>
                        <DataTemplate x:DataType="model:FoodItem">
                            <TextCell Text="{Binding FoodName}" Detail="{Binding Kcal, StringFormat='{0} kcal'}" DetailColor="Black" TextColor="Black"></TextCell>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
                
                <ActivityIndicator Color="LightSeaGreen" IsRunning="{Binding IsBusy}"></ActivityIndicator>

                <toolkit:Expander>
                    <toolkit:Expander.Header>
                        <Label FontSize="Small" Text="{Binding ConsumedFoodItems.Count, StringFormat='Todays Intake ({0} foods)'}"></Label>
                    </toolkit:Expander.Header>
                    <VerticalStackLayout>
                        <CollectionView ItemsSource="{Binding ConsumedFoodItems}">
                            <CollectionView.ItemsLayout>
                                <LinearItemsLayout Orientation="Vertical"/>
                            </CollectionView.ItemsLayout>
                            <CollectionView.ItemTemplate>
                                <DataTemplate x:DataType="model:FoodItem">
                                    <Grid Padding="10,0">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"></ColumnDefinition>
                                            <ColumnDefinition Width="*"></ColumnDefinition>
                                        </Grid.ColumnDefinitions>
                                        <Label Grid.Column="0" VerticalOptions="Center" FontSize="Small" Text="{Binding FoodName}"/>
                                        <Label Grid.Column="1" HorizontalTextAlignment="End" FontSize="Small" Text="{Binding Gram, StringFormat='{0} Grams'}"/>
                                    </Grid>
                                </DataTemplate>
                            </CollectionView.ItemTemplate>
                        </CollectionView>
                    </VerticalStackLayout>
                </toolkit:Expander>
            </VerticalStackLayout>
        </VerticalStackLayout>
    </ScrollView>
</ContentPage>
