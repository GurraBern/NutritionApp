<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="NutritionApp.MVVM.Views.FoodDetailPage"
             xmlns:viewmodel="clr-namespace:NutritionApp.MVVM.ViewModels" 
             x:DataType="viewmodel:FoodDetailViewModel"
             xmlns:model="clr-namespace:NutritionApp.MVVM.Models"
             xmlns:customComponents="clr-namespace:NutritionApp.MVVM.Views"
             xmlns:toolkit="http://schemas.microsoft.com/dotnet/2022/maui/toolkit"
             BackgroundColor="{DynamicResource White}"
             Title="FoodDetailPage">

    <VerticalStackLayout Margin="10,5" Spacing="10">
        <Label Text="{Binding FoodItem.Name}" FontSize="Medium" HorizontalOptions="Center"></Label>
        <HorizontalStackLayout>
            <Border>
                <Entry Text="{Binding Amount}"></Entry>
                <Border.StrokeShape>
                    <RoundRectangle CornerRadius="5"></RoundRectangle>
                </Border.StrokeShape>
            </Border>
            <Label Text="Gram" Margin="5,0" VerticalOptions="Center" FontSize="Small"></Label>
        </HorizontalStackLayout>


        <toolkit:Expander IsExpanded="True">
            <toolkit:Expander.Header>
                <Label FontSize="Small" Text="Primary Nutrients"></Label>
            </toolkit:Expander.Header>
            <VerticalStackLayout>
                <customComponents:CustomProgressBar Title="{Binding FoodItem.Calories, StringFormat='Kcal {0}'}" ProgressA="{Binding PotentialKcalProgress}" ProgressB="{Binding KcalProgress}" ProgressBarAColor="LightSeaGreen" ProgressBarBColor="LimeGreen"/>
                <customComponents:CustomProgressBar Title="Protein" ProgressA="{Binding PotentialProteinProgress}" ProgressB="{Binding ProteinProgress}" ProgressBarAColor="LightSeaGreen" ProgressBarBColor="LimeGreen"/>
                <customComponents:CustomProgressBar Title="Carbs" ProgressA="{Binding PotentialCarbsProgress}" ProgressB="{Binding CarbsProgress}" ProgressBarAColor="LightSeaGreen" ProgressBarBColor="LimeGreen"/>
                <customComponents:CustomProgressBar Title="Fat" ProgressA="{Binding PotentialFatProgress}" ProgressB="{Binding FatProgress}" ProgressBarAColor="LightSeaGreen" ProgressBarBColor="LimeGreen"/>
            </VerticalStackLayout>
        </toolkit:Expander>

        <Label Text="Nutritional Information (100g)" FontSize="Small" FontAttributes="Bold"></Label>
        <Border Padding="15">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition></RowDefinition>
                    <RowDefinition></RowDefinition>
                    <RowDefinition></RowDefinition>
                    <RowDefinition></RowDefinition>
                    <RowDefinition></RowDefinition>
                </Grid.RowDefinitions>

                <Label Grid.Row="0" Text="Calories" FontAttributes="Bold"></Label>
                <Label Grid.Row="0" Text="{Binding FoodItem.Calories, StringFormat='{0} kcal'}" HorizontalOptions="End"></Label>

                <Label Grid.Row="1" Text="Protein" FontAttributes="Bold"></Label>
                <Label Grid.Row="1" Text="{Binding FoodItem.Protein, StringFormat='{0}g'}" HorizontalOptions="End"></Label>

                <Label Grid.Row="2" Text="Carbs" FontAttributes="Bold"></Label>
                <Label Grid.Row="2" Text="{Binding FoodItem.Carbohydrates.Carbohydrate, StringFormat='{0}g'}" HorizontalOptions="End"></Label>

                <Label Grid.Row="3" Text="Fat" FontAttributes="Bold"></Label>
                <Label Grid.Row="3" Text="{Binding FoodItem.Fats.TotalFat, StringFormat='{0}g'}" HorizontalOptions="End"></Label>
            </Grid>
            <Border.StrokeShape>
                <RoundRectangle CornerRadius="5"/>
            </Border.StrokeShape>
        </Border>

        <Button Margin="0,5" Command="{Binding AddFoodCommand}" Text="ADD"></Button>

    </VerticalStackLayout>
</ContentPage>